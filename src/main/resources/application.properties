spring.application.name=backend
server.port=${PORT:8080}

microservice.ia.url=${MICROSERVICE_IA_URL}

# Database configuration >:3
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.leak-detection-threshold=30000
spring.datasource.hikari.max-lifetime=1800000

#JPA Stuff
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Redis Configuration
redis.base.url=${REDIS_BASE_URL:https://helped-cockatoo-53634.upstash.io}
redis.bearer.token=${REDIS_BEARER_TOKEN:AdGCAAIncDE3ZTEwN2I0ODZjMTU0YjU5OWVmNzdmYzU3MDYxYWFlMHAxNTM2MzQ}